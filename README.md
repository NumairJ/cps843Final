# cps843Final
Measuring metrics for YOLOv5 models
Getting Started:
Python 3 installed

On Linux machine:
1. 
``` bash
pip install -r requirements.txt
```

2. 
``` bash
sudo apt-get update
sudo apt-get install -y libgl1
```