# cps843Final
Measuring metrics for YOLOv5 models

Getting Started:

Python 3 installed

On Windows/Linux machine:
1. 
``` bash
pip install -r requirements.txt
```

on Linux machine if importing ultralytics gives an error relating to libgl1
2. 
``` bash
sudo apt-get update
sudo apt-get install -y libgl1
```
